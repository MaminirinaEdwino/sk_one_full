<form method="post" action="{{ path('app_tache_normale_delete', {'id': tache_normale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tache_normale.id) }}">
    <button class="btn">Delete</button>
</form>
