<form method="post" action="{{ path('app_commentaire_demande_delete', {'id': commentaire_demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire_demande.id) }}">
    <button class="btn">Delete</button>
</form>
