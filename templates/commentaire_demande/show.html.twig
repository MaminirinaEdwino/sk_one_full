{% extends 'base.html.twig' %}

{% block title %}CommentaireDemande{% endblock %}

{% block body %}
    <h1>CommentaireDemande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_demande.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire_demande.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_demande_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_demande_edit', {'id': commentaire_demande.id}) }}">edit</a>

    {{ include('commentaire_demande/_delete_form.html.twig') }}
{% endblock %}
