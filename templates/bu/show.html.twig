{% extends 'base.html.twig' %}

{% block title %}BU{% endblock %}

{% block body %}
    <h1>BU</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ b_u.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ b_u.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_b_u_index') }}">back to list</a>

    <a href="{{ path('app_b_u_edit', {'id': b_u.id}) }}">edit</a>

    {{ include('bu/_delete_form.html.twig') }}
{% endblock %}
