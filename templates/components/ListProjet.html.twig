<div{{ attributes }} class="" >
    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Statut</th>
                <th>Archive</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projet in list %}
            <tr>
                <td>{{ projet.id }}</td>
                <td>{{ projet.nom }}</td>
                <td>{{ projet.status }}</td>
                <td>{{ projet.archive ? 'Yes' : 'No' }}</td>
                <td>{{ projet.dateDebut ? projet.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ projet.dateFin ? projet.dateFin|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ projet.description }}</td>
                
                <td>
                    <a href="{{ path('app_projet_show', {'id': projet.id}) }}">show</a>
                    <a href="{{ path('app_projet_edit', {'id': projet.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    <div class="row">
        {% for projet in list %}
    <div class="project-card {{ projet.status | lower == 'en attente'? 'pending' : '' }}
    {{ projet.status | lower == 'en cours'? 'in-progress' : '' }}
    {{ projet.status | lower == 'terminée'? 'completed' : '' }}
    {{ projet.status | lower == 'bloquée'? 'blocked' : '' }}" data-project-id="4" data-deadline="2025-09-10" data-title="Système de paiement" data-description="Intégration d'un nouveau processeur de paiement (Stripe/PayPal) et tests unitaires.">
        <div class="project-header">
            <h3 class="project-title">{{ projet.nom }}</h3>
        </div>
        <div class="project-body">
            <p class="project-description">
                {{ projet.description }}
            </p>
        </div>
        <div class="project-date-info">
            <p class="deadline">Deadline: {{ projet.dateFin ? projet.dateFin|date('Y-m-d H:i:s') : '' }}</p>
            <span class="time-remaining"> {{ projet.dateDebut ? projet.dateDebut|date('Y-m-d H:i:s') : '' }} </span>
        </div>
        <div class="project-footer">
            <span class="project-status {{ projet.status | lower == 'pending'? 'En attente' : '' }}
            {{ projet.status | lower == 'en cours'? 'in-progress' : '' }}
            {{ projet.status | lower == 'terminée'? 'completed' : '' }}
            {{ projet.status | lower == 'bloquée'? 'blocked' : '' }}" onclick="">
                {{ projet.status | lower == 'en attente'? 'En attente' : '' }}
                {{ projet.status | lower == 'en cours'? 'en cours' : '' }}
                {{ projet.status | lower == 'terminée'? 'terminée' : '' }}
                {{ projet.status | lower == 'bloquée'? 'bloquée' : '' }}
            </span>

            <script>


            </script>
            <a class="project-action-btn details-btn" data-project-id="4" href="{{ path('app_projet_show', {'id': projet.id}) }}">Détails</a>
            {# <a class="project-action-btn delete-btn" data-project-id="4" href="{{ path('app_projet_edit', {'id': projet.id}) }}>Supprimer</a> #}

            
        </div>
    </div>
    {% endfor %}
    </div>
    <div id="modal-projet-status" >

    </div>
    <div class="pagination-container">
        <div class="per-page-selector">
            <label for="per-page-select">Éléments par page :</label>
            <div class="select-wrapper">
                <select id="per-page-select" class="custom-select" data-model="on(change)|limit" data-action="live#action" data-live-action-param="nextPage" data-live-page-param="{{ 1 }}">
                    <option value="10">10</option>
                    <option value="25">25</option>
                    <option value="50">50</option>
                    <option value="100">100</option>
                </select>
                <span class="select-arrow">▼</span>
            </div>
        </div>
        
        <div class="page-navigation">
            {% if page>1 %}
            <button class="nav-btn prev-btn" data-action="live#action" data-live-action-param="nextPage"
            data-live-page-param="{{ page - 1 }}">Précédent</button>
            {% endif %}
            <span class="page-number-text">Page {{ page }} sur {{ total }}</span>
            {% if page<total %}
            <button class="nav-btn next-btn" data-action="live#action" data-live-action-param="nextPage"
            data-live-page-param="{{ page + 1 }}">Suivant</button>
            {% endif %}
        </div>
    </div>
</div>
