<form method="post" action="{{ path('app_jour_de_travail_delete', {'id': jour_de_travail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jour_de_travail.id) }}">
    <button class="btn">Delete</button>
</form>
