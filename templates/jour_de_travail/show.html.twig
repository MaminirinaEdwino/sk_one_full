{% extends 'DashBoard.html.twig' %}

{% block title %}JourDeTravail{% endblock %}

{% block body %}
    <h1>JourDeTravail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jour_de_travail.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ jour_de_travail.jour ? jour_de_travail.jour|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jour_de_travail_index') }}">back to list</a>

    <a href="{{ path('app_jour_de_travail_edit', {'id': jour_de_travail.id}) }}">edit</a>

    {{ include('jour_de_travail/_delete_form.html.twig') }}
{% endblock %}
